# Use an official Node.js runtime as a parent image
FROM node:16

# Set the working directory inside the container
WORKDIR /usr/src/app

# Copy the package.json and package-lock.json
COPY package*.json ./

# Install project dependencies (this step assumes you have a package.json file)
RUN npm install

# Copy the rest of your application code
COPY . .

# Expose the port (if your application serves on a specific port)
EXPOSE 3000