FROM jenkins/jenkins:lts

# Install Git, mailutils, and other necessary packages
USER root
RUN apt-get update && \
    apt-get install -y git mailutils curl && \
    apt-get clean

# Return to the Jenkins user
USER jenkins

# Expose the Jenkins port
EXPOSE 8080
